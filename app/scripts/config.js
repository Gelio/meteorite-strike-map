var config = {
    width: 900,
    height: 600,
    mapDataUrl: './countries.geo.json',
    meteoriteDataUrl: './meteorite-strike-data.json',
    baseScale: 300           // default should be 90
};

module.exports = config;